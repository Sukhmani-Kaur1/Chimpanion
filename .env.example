# Admin area (/admin). Both are required — without them the admin area stays locked.
# ADMIN_SECRET signs the session cookie: use a long random string, e.g. `openssl rand -hex 32`.
ADMIN_PASSWORD=
ADMIN_SECRET=
